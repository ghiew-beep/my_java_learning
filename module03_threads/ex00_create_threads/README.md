# Basic Java Threads Workflow

This project demonstrates two common ways to create and run threads in Java:

1. **Extending the `Thread` class**
2. **Implementing the `Runnable` interface (via lambda expression)**

---
